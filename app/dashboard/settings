"use client";
import React from 'react';
import { useRouter } from 'next/navigation';

export default function SettingsPage() {
  const router = useRouter();

  return (
    <div className="min-h-screen bg-[#0a0b14] text-white p-6 font-sans">
      <div className="flex items-center gap-4 mb-10">
        <button onClick={() => router.back()} className="w-10 h-10 bg-zinc-900 rounded-full flex items-center justify-center">←</button>
        <h1 className="text-xl font-black uppercase italic tracking-tighter">Paramèt</h1>
      </div>

      <div className="space-y-3">
        {['Pwofil', 'Sekirite Modpas', 'Verifikasyon Idantite (KYC)', 'Langue', 'Èd & Sipò'].map((item) => (
          <div key={item} className="bg-zinc-900/50 p-6 rounded-3xl border border-white/5 flex justify-between items-center group cursor-pointer hover:bg-zinc-800 transition-all">
            <span className="font-bold text-sm text-zinc-300">{item}</span>
            <span className="text-zinc-600 group-hover:text-red-600 transition-all">→</span>
          </div>
        ))}

        <button className="w-full mt-10 p-6 rounded-3xl border border-red-900/30 text-red-600 font-black uppercase text-xs tracking-widest hover:bg-red-600 hover:text-white transition-all">
          Dekonekte
        </button>
      </div>
    </div>
  );
}